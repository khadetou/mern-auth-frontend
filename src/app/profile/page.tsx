"use client";

interface ProfileProps {
  propName: string;
}
export default function Profile({}: ProfileProps) {
  return (
    <div>
      <h1>Hello Root and MetaData Profile</h1>
    </div>
  );
}
